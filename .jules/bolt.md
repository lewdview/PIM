## 2026-08-19 - [Extract high frequency components]
**Learning:** Extract components that rely on high frequency updates to avoid O(N) re-renders, especially when granular selectors aren't possible.
**Action:** Extract the progress bar and time updates into their own component.
